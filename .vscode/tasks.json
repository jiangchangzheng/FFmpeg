{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug",
            "type": "shell",
            "command": "cd ${workspaceFolder} && make -j8 && make install"
        }
    ]
}